IMG_WIDTH = 640
IMG_HEIGHT = 480
COM = "COM3"
CAMERA_INDEX = 1

# Setting this to True means that the program will only use the YOLO model to detect the targets, repeatedly, with memory
# Setting this to False means that the program will use the YOLO model once to detect the targets, and then detect the changes
useJustYOLO = False
